name = "orchid-api"
main = "src/index.js"
compatibility_date = "2024-03-20"

# Bindings for the D1 Database
[[d1_databases]]
binding = "DB"
database_name = "orchid-bookings"
database_id = "5fc5fd5e-3e4d-4fc0-94af-4b18b55c5d7d" # استبدل هذا بالـ ID بعد إنشاء قاعدة البيانات
migrations_dir = "migrations"

[vars]
# سيتم استخدام هذا المتغير كـ Bearer Token للمصادقة
ADMIN_PASSWORD = "Admin@12345"
